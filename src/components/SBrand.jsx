import React from "react";

const SBrand = ({ brand }) => {
  // const { img } = brand.props
  return (
    <div>
      <img src={brand} alt="brand name" />
    </div>
  );
};

export default SBrand;
