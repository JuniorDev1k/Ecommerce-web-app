import React from "react";
// responsible for user Profile shoping card and username, profile picture  ||
//  if there is no user , no user data + empty card , click navigate to signup/login
const ProductsUserProfile = () => {
  return <div>userInfos : Shoping card and profile data</div>;
};

export default ProductsUserProfile;
